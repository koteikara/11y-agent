# コンテンツ移行: 機械取込みがうまくいかないパターン（ユーザー共有メモ）

- 記録日: 2026-09-18
- 出所: ユーザー（中川）からチャットで共有された、SMART L-Gov CMS への取込作業の現場知見。
- 位置づけ: 原文のまま記録する。設計や課題化はまだ行っていない（別途 project-issue-candidates.md への反映を検討）。
- 注記（ユーザーより）: リンク先URLは not found になっているもの、更新されているものもあるが、CMS側で取込履歴が残っている可能性があるのでそのまま掲載する。

## 1. 取り込めない／取り込めてもパーツにハマらない発生条件

### ファイルリンクやリンクが WYSIWYG に入ってしまう
- 条件: リストの中にリンク／ファイルリンクがある時、「リスト＝WYSIWYG」が優先されてしまう。
- 取込前: https://www.town.koryo.nara.jp/contents_detail.php?co=kak&frmId=5993
- 取込後: https://www01.migrate.smart-lgov.jp/torikomi/77/koryo/6822.html

### ファイルリンクが、リンク付き画像で取り込まれてしまう
- 条件: アイコン画像がリンク前にある時、アイコン画像を除外指定していなければ発生することがある。除外指定していれば発生しない。
- 条件: ファイルリンクにアイコンが組み込まれている（CSSではなく直接 img タグでアイコンがついている）形式の場合に、テキストリンクが除去されアイコンのみが移行される。
- 取込前: https://www.town.tochigi-nakagawa.lg.jp/life/kosodate_kyouiku/2025-0324-1009-105.html
- 取込後: https://www01.migrate.smart-lgov.jp/torikomi/78/nakagawa/9319.html

### 取込元の HTML が特殊なため取り込めない: ファイルの拡張子が URL に含まれない
- 取込前: https://www.town.tochigi-nakagawa.lg.jp/life/fukushi_kaigo/2020-0219-1001-25.html
- 取込後: https://www01.migrate.smart-lgov.jp/torikomi/78/nakagawa/9313.html
- 原因: 画像URLが `data:image/png;base64,/9j/4A....` となっており、拡張子が png でないため画像として取り込まれない。

### 取込元の HTML が特殊なため取り込めない: CSS 側で画像表示している
- 条件: 画像が HTML ではなく CSS の背景画像として表示されているため取り込めない（コンテンツの一部なので要取込）。
- 移行元: https://www.town.wake.lg.jp/children/englishQuiz/
- 取込後: http://www03.migrate4.smart-lgov.jp/torikomi_test/77/wake/239.html

### SMART L-Gov CMS 非対応形式のファイル
- 非対応形式のファイルのため、取り込めず／手動アップも出来ず。
- 例 ODT 形式: https://www.townkamiita.jp/docs/2013103000011/
- 例 webp 画像: https://iju.go-iijima.nagano.jp/information/4847/

### 取込元の HTML が特殊なため取り込めない: DOM で書かれている
- （具体例なし。JavaScript で DOM を組み立てているページと解釈）

### 画像やファイルの URL 末尾に不要なソースがあるために画像・ファイルとして認識されない
- 取込前: https://kamisu-pr.jp/2021/11/10/r3senningarou/
- 取込後: http://www01.migrate.smart-lgov.jp/torikomi/77/kamisushi_kamisumika_pre/5833.html
- 原因: `file_size="250104"` や `data-foreign-domain=""` といった不要な属性が入っている。

## 2. 実際のページ取込で起きるよくあるエラー（頻度の高い順）

### ① Not found のファイルや画像
- ページ内に not found の画像やファイルがあるとエラーとなり、ページとしては取込に失敗する。
- 現状: エラー検出後に手動で対象箇所のファイルを1つずつ削除して取り込んでいる。
- 要望: 自動的にリンクを除去して取り込んでほしい（ファイルテキストはテキストとして取り込むべきかは要検討）。

### ② SVG 画像ファイルが検出されない
- 画像として検出されず、それでいて取込時に画像登録失敗エラーとして検出される。
- いままさに遠野市で起きている: https://www.city.tono.iwate.jp/index.cfm/45,43562,242,472,html

### ③ 外部リンク系（取込負荷への影響が大きい）
- 主に「ふるさと納税（さとふる等）」や「Adobe Reader ダウンロード」といった特定の外部リンクがあるページは、取込時にエラーにもならず数万秒作業をし続ける状態になるため強制停止している。
- 強制停止するため、裏で取込成功したページがのちに登録されており、二重登録が問題にもなっている。
- 上記2つは明確に確認済み。稀に別のサイトでも同様の現象が起きているので、共通点として「外部リンク」と表現している。

### ④ 機種依存文字
- ページ内に機種依存文字（規格外の文字コード？ 縦長の長方形っぽい見た目になるもの）が含まれていると取込結果がエラーとなり取り込めない。
- ここ数年は見なくなっている（何かしらのバージョンアップで副作用的に解消された可能性）。数年前までは非常に多くのエラーを検出していた。
- ページ内で原因の文字を探す難易度が高く、ざっくりそれっぽい箇所を削除して取り込んでいた。事前に検知できるとスムーズ。

## 3. 「これが出来るとだいぶスピードアップできる」系

### 画像が複数横並びになっている時、2枚並びまたは3枚並びのパーツに取り込む
- HTML複数行に取り込まれる素材は「外に出す」設定で外に出せるが、画像だけはファイル・リンクと違って「横並び」系のパーツがあるため、パーツの入れ直し作業が発生する。
- 横並び画像であることを判定でき、さらに2枚並び・3枚並びのどれにあたるかも判定できれば、画像の多いページでの入れ替え作業をまるごと無くせる（「時間がかかるページ」の一角）。

### ファイルリンクは、表の中か否かで取り込み方を変えられる
- 「外に出す」設定で取り込んだ時に困るのが表内にあるファイルリンクで、外に出たファイルリンクを表内で再設定（入れ直す）作業が発生する。
- 表内ファイルを優先するなら「外に出さない」設定が望ましいが、全部が表内ファイルというわけではない。
- 要望: 「基本は外に出すが、表内にあるリンク・ファイルリンクは出さない」という取り込み方を案件ごとに選択できると、表内のファイルリンクの入れ直し作業を無くせる。

### HTML複数行内ファイルリンクにファイル種別と容量表示を追加する
- ファイルリンクパーツは自動的に「（PDF：20.3MB）」のようにファイル種別と容量を表示するが、HTML複数行内に取り込まれたファイルリンクには自動表示されないため、再度ファイル挿入し直している（ファイル挿入するとリンクテキストの一部として表示される）。
- 自動表示されるなら、取り込まれた状態からリンクテキストを適切な文字列に打ち換えるだけで（ほぼ）済む（「時間がかかるページ」の一角）。

## 4. 着手条件の検証（2026-09-18、Fable）

各項目を課題として起票し解消できるかを、次の4条件で見た。

- 発生条件: 取込前のHTMLから機械的に判定できる書き方になっているか。
- サンプル: 再現に使うHTMLをこのリポジトリから参照できるか。
- 期待結果: ツールが何をすれば「解消」なのかが決まっているか（検知して知らせる、取込前にHTMLを整える、CMS取込ツール側の変更のどれか）。
- 担当: ツール側（goal2-app）で閉じるか、CMS取込ツールの仕様に依存するか。

### 4.1 前提として確認したこと

- ツールは、CMS取込ツールの「解析まで」の出力を受け取り、最終HTMLを「取り込み範囲編集」へ戻す位置にある（`goal2-app/public/verification-guide.html` 4章・5章）。パーツへの分解（WYSIWYG、ファイルリンクパーツ、画像パーツ）はCMS取込ツールが行い、その規則はリポジトリに無い。
- 遠野市の指摘5・9・14（改行が落ちる、画像の並び、メールの件名）は「CMS取込側」として対象外にした前例がある（`goal2-app/TONO_FEEDBACK_FIX_INSTRUCTIONS.md` 1章）。本メモの3-1はこの指摘9と同じ内容である。
- ツールには次の下地がある。リンク先へHTTPで問い合わせる `/api/link-title`（`server.js`、状態コードを返す）。画像取得の `fetchImageAsBase64`（受け付けるのは jpeg・png・webp・gif で、svg は受け付けない）。同じまとまりに画像が2枚以上ある候補 `image.multiple-images` と、画像とテキストの横並び候補 `image.image-text-layout`（どちらも要確認）。ファイルアイコンの判定（指摘12の対応）。Goal 3 の Adobe Reader 案内の除外 `isAdobeReaderNotice()`。
- KBには、リンク切れの扱い（`rules/link/link-broken.md`: 報告し、文脈でリンク削除かテキスト化、URLは変えない）、ファイル表示テキスト（`rules/file/file-display-text.md`: 種別・容量はCMSが自動表示するので削除）、複数画像の並び（`rules/image/multiple-images.md`: 横並びは最大3枚）、移行不要の箇所（`reference/migration-not-needed.md`: Adobe Reader のDLリンク）、禁則文字・機種依存文字チェック（`cms/special-char-check.md`: 更新・承認申請時のチェック）がある。
- 掲載URL 13件をこの環境（クラウド）から確認した結果は次のとおり。内容を確認できたのは5件で、元ページ3件（上板町、神栖市、遠野市）と取込後2件（那珂川町の2件）。到達できなかったものは、ユーザーのブラウザ（VPN）で開いてHTMLを保存する必要がある。

| 項目 | URL | 結果 | 確認できたこと |
| --- | --- | --- | --- |
| 1-1 | 広陵町（元） | 408 と 503。「このページは表示できません。インシデント ID」のエラーページを返す（WAF に止められている） | 未確認。ブラウザで開いて保存が要る |
| 1-1 | 広陵町（取込後 6822） | 404 | 消えている。CMS側の取込履歴が要る |
| 1-2 | 那珂川町（元 105） | 404（サイトが作り替えられ「ページ情報」ページになる） | 取込前のHTMLは取れない |
| 1-2 | 那珂川町（取込後 9319） | **200** | PDF リンク4件がすべて `<figure class="img-item"><a href="…pdf" target="_blank"><img alt="PDFファイル" src="…/pdf_04121842.gif"></a></figure>` になっている。リンク文言は無く、アイコンだけが画像パーツとして入った。メモの記述どおり |
| 1-3 | 那珂川町（元 25） | 404 | 取込前のHTMLは取れない |
| 1-3 | 那珂川町（取込後 9313） | **200** | 本文（ヘルプマークの説明）は文だけで、画像が1枚も無い。`data:` 画像が落ちたことと整合する |
| 1-4 | 和気町（元） | 接続が切れる。別経路では 404 | 消えている可能性が高い |
| 1-4 | 和気町（取込後 239、migrate4 の torikomi_test） | CMS のログイン画面へリダイレクト（証明書は自己署名） | ログインが要る。ユーザーのブラウザなら見える |
| 1-5 | 上板町（元） | 接続が切れる。別経路では **取得できた** | 様式第3号・第4号・第8号の3件が `.odt` と `.pdf` の2本立てで置かれている。ODT の例は確認できた |
| 1-5 | 飯島町（元） | 404（「ページが見つかりませんでした」） | webp の例は未確認 |
| 1-7 | 神栖市（元） | **200** | いまは `file_size` も `data-foreign-domain` も無い（`data-wpel-link="internal"` はある）。取込時のHTMLと違う |
| 1-7 | 神栖市（取込後 5833） | 404 | 消えている。CMS側の取込履歴が要る |
| 2-② | 遠野市（元） | **200** | `<img alt="マイナポータルロゴ画像" src="/images/content/43562/logo.svg">` があり、SVG の例は確認できた |

### 4.2 項目ごとの判定

| 項目 | 発生条件 | サンプル | 期待結果 | 担当 | 判定 |
| --- | --- | --- | --- | --- | --- |
| 1-1 リスト内のリンクが WYSIWYG に入る | 明確（`li` の中にリンク・ファイルリンク） | 無し（元・取込後とも到達不可） | 未定。リストを崩してパーツに出すのか、WYSIWYG のままでよいのか | 分解はCMS側。件数の検知はツール側 | 不足（期待結果、CMSのパーツ規則） |
| 1-2 ファイルリンクがリンク付き画像になる | 明確（`a` の直前または中にアイコン `img`） | 取込後の形は那珂川町 9319 で確認済み（リンク文言が消え、アイコンだけが画像パーツになる）。取込前のHTMLは 404 なので人工HTMLで作る | 取込前にアイコン `img` を外す（KBの「種別はCMSが自動表示」と整合） | ツール側で閉じる。指摘12のアイコン判定を再利用できる | 揃っている |
| 1-3 `data:` 形式の画像 | 明確（`src` が `data:` スキーム） | 取込後の形は那珂川町 9313 で確認済み（画像が落ちて文だけ残る）。取込前は 404 なので人工HTMLで作る | 検知は明確。ファイルに書き出して手動アップするか、外して報告するかが未定 | 検知はツール側。ファイル化と登録は手作業 | 検知は揃っている。処理方針だけ決める |
| 1-4 CSS の背景画像 | 本文HTML断片だけでは判定できない（CSSを読む必要がある）。「コンテンツの一部」の判断基準も無い | 無し（接続不可） | 未定（`img` に置き換えるのか、手作業か） | Goal 3（ページ全体HTML）の段階ならCSSを参照できる可能性 | 不足（CSSの取得方法、判断基準、期待結果） |
| 1-5 非対応形式（ODT、webp） | 拡張子で判定できるが、CMSの対応形式一覧が無い | ODT は上板町のページで確認済み（3様式が `.odt` と `.pdf` の2本立て）。webp は飯島町が 404 で未確認 | 事前に検知して報告。変換（PDF化・jpg化）は自治体判断 | 検知はツール側 | 対応形式一覧（許可する拡張子・MIME）があれば揃う |
| 1-6 DOM で書かれている | 例が無く、条件が定義できない（JavaScript で本文を生成するページと推定） | 無し | 未定 | 未定 | 不足（例が要る） |
| 1-7 URL 末尾の不要ソース | 属性名は具体的だが、移行元の現在のHTMLには無い。取込処理の途中で付いた可能性がある。「URL末尾」と「属性」の関係も要確認（`href` の中に混ざるのか、`a` の属性なのか） | 到達できるが再現しない | 未定 | 未定（CMS取込ツール側の可能性） | 不足（取込時のHTMLか取込履歴） |
| 2-① Not found のファイル・画像 | 明確（HTTP 404 など）。判定にネットワーク到達性が要る | 無し。人工的に再現できる | リンクはKB（link-broken）に従える。画像の not found はKBに無い。テキスト化するかは「要検討」と明記 | ツール側。`/api/link-title` の仕組みで事前検知できる。Cloud Run から自治体サイトへの到達性と大量アクセスの運用条件が要る | 概ね揃っている。決めるのは画像の扱いと、テキスト化の規則 |
| 2-② SVG 画像 | 明確（`.svg` / `image/svg+xml`） | 遠野市のページで確認済み | CMSが svg 非対応なら事前検知して報告。png 化は手作業 | 検知はツール側 | 揃っている（1-5の対応形式一覧で svg の扱いを確認するだけ） |
| 2-③ 外部リンクで取込が止まらない | 不明確（さとふる・Adobe Reader は確認済み、他は稀）。原因はCMS取込ツール側（リンク先取得のタイムアウト無し？） | 無し | 根本はCMS側（タイムアウト、二重登録）。ツール側では「移行不要リンクの除去」（KBにある）と「外部リンク一覧」で先回りできる | 主にCMS側 | 不足（CMS側の原因、止まるURLの条件、取込ログ） |
| 2-④ 機種依存文字 | 「規格外の文字コード」「縦長の長方形」だけでは文字集合が決まらない（Shift_JIS の拡張、外字、U+FFFD などが候補） | 無し（数年前） | 事前に文字と位置を示す。明確 | ツール側。CMS の禁則文字・機種依存文字チェックの文字集合を流用できる可能性 | 文字集合（またはCMSの判定API）があれば揃う。現在は起きていないので優先度は低い |
| 3-1 横並び画像を 2枚・3枚パーツへ | 「横並び」の判定条件が未定義（同じ `p`・`div` に並ぶ、表の1行、float、flex）。枚数の区別は機械的に決まる | 無し。2026年8月の検証記録に同じ現象がある | CMS取込ツールがどんなHTMLを受け取れば 2枚・3枚パーツにするか（取込ツールの入力仕様）が無い。無ければツールは「判定して知らせる」まで | 判定はツール側、パーツ化はCMS側 | 不足（判定条件と、取込ツールの入力仕様） |
| 3-2 表内のファイルリンクは外に出さない | 明確（`table` 内のファイルリンク） | 無し | 「外に出す」設定の粒度追加。CMS取込ツールの設定仕様 | CMS側。ツール側は表内ファイルリンクの件数を出して判断材料にできる | CMS側の変更が本体。「外に出す」設定の仕様書が無い |
| 3-3 WYSIWYG 内ファイルリンクに種別・容量 | 明確（WYSIWYG に残るファイルリンク） | 無し | CMS側で自動表示するのが本筋。ツール側で付けるなら、KB（種別・容量は削除）と逆の処理になるので整合を決める必要がある。容量取得にはファイルへの HEAD 要求が要る | CMS側。ツール側なら「WYSIWYG に残るリンクの判定」と「容量取得」の2つが要る | 不足（CMS側かツール側かの判断、KBとの整合） |

### 4.3 横断して足りないもの

1. CMS取込ツールの仕様。パーツへの分解規則、「外に出す」設定の内容、対応ファイル形式の一覧、機種依存文字の定義、取込エラーの種類とログ。14項目のうち10項目がこれに依存する。
2. サンプルHTMLの保存。URLは消えるか変わる（13件中、内容を確認できたのは5件。神栖市は内容が変わっている）。取込前のHTMLと、取込ツールが受け取った時点のHTMLを、フィクスチャとしてリポジトリに置く。CMS側に取込履歴が残っていれば、そこから取る。
3. 期待結果の分類。各項目を「検知して知らせる」「取込前にHTMLを整える」「CMS側に依頼する」に分ける。ツール側で今すぐ着手できるのは検知系で、1-2、1-3、1-5、2-①、2-②、2-④（文字集合があれば）、3-2（件数）である。
4. 件数と頻度。①〜④は頻度順だが件数が無い。優先度を付けるには、直近の案件での発生件数か、ページ数に対する割合が要る。
5. スコープの判断。「取込前の事前検知」を Goal 1 の前処理としてプロジェクトの範囲に入れるかどうか。遠野市の指摘5・9・14 を「CMS取込側」で外した前例と整合させる必要がある。

### 4.4 いま着手できる形

- 1-2（アイコン付きファイルリンク）と 2-②（svg）は、条件・期待結果・担当がそろい、取込後の形（1-2）と元ページ（2-②）も確認できたので起票できる。
- 2-①（not found）は、画像の扱いとテキスト化の規則を決めれば起票できる。検知の基盤は `/api/link-title` にある。
- 1-3（`data:` 画像）と 1-5（非対応形式）は、検知だけなら起票できる。処理方針と対応形式一覧は起票後に埋める。
- 残り（1-1、1-4、1-6、1-7、2-③、2-④、3-1、3-2、3-3）は、CMS取込ツールの仕様か例が無いと起票しても解消条件が書けない。
